{
  "name": "PHP-project-cogip",
  "version": "1.0.0",
  "description": "## Objectives : * Using different `URL`. * `Sanitization` to avoid SQL. * `Validation` of Data. * `Relational` database. * Correct `join` in SQL. * Using `Aliases` in SQL request. * Implementation of a `GRUD`.",
  "main": "index.js",
  "scripts": {
    "scss": "node-sass --watch src/assets/scss --output src/assets/css --source-map true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tselawi/COGIP.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Tselawi/COGIP/issues"
  },
  "homepage": "https://github.com/Tselawi/COGIP#readme",
  "dependencies": {
    "node-sass": "^5.0.0"
  }
}
